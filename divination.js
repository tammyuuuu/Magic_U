(()=>{const $=id=>document.getElementById(id),stages=[...document.querySelectorAll('.reading-stage')],positions=[['过去','Past','它可能留下了什么影响？'],['现在','Present','它怎样描述此刻？'],['未来','Future','它可能带来什么趋势？']];let deckOrder=[],cards=[],notes=['','',''],interpretIndex=0;const show=id=>stages.forEach(stage=>stage.hidden=stage.id!==id);function config(){const d=DECKS.find(x=>x.name==='维特塔罗')||DECKS[0];return{path:d.path.replace(/\/+$/,''),format:d.format||'png',back:d.backFile||'back.png',startAt:Number(d.startAt)||0,count:Number(d.cardCount)||78}}const cardPath=id=>{const d=config();return`${d.path}/${d.startAt+id}.${d.format}`},backPath=()=>{const d=config();return`${d.path}/${d.back}`},identity=id=>TAROT_CARDS.find(card=>card.id===id);function shuffle(){const ids=Array.from({length:config().count},(_,i)=>i);for(let i=ids.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[ids[i],ids[j]]=[ids[j],ids[i]]}return ids}function flipper(item){return`<div class="card-flipper"><div class="card-face card-back"><img src="${backPath()}" alt="牌背"></div><div class="card-face card-front ${item.orientation==='reversed'?'reversed':''}"><img src="${cardPath(item.id)}" alt="${identity(item.id).nameZh}"></div></div>`}function boardMarkup(reveal=false){return positions.map((p,i)=>{const item=cards[i];return`<div class="card-slot ${!reveal&&i===cards.length?'current':''}"><div class="${reveal?'reading-card':'slot-frame'} ${item?'filled':''}" data-number="${i+1}" data-index="${i}">${item?(reveal?flipper(item):`<img src="${backPath()}" alt="牌背">`):''}</div><strong>${p[0]}</strong><small>${p[1]}</small></div>`}).join('')}function renderBoard(){$('spreadBoard').innerHTML=boardMarkup(false);$('drawHint').textContent=cards.length<3?`为「${positions[cards.length][0]}」抽一张牌。`:'三张牌已经进入牌阵。'}function renderFan(){const wrap=$('fanWrap'),visible=deckOrder.slice(0,13),center=(visible.length-1)/2;wrap.innerHTML=visible.map((id,i)=>{const rel=i-center,angle=rel*5.2,x=rel*19,y=Math.abs(rel)*2;return`<button class="fan-card" type="button" data-id="${id}" style="transform:translateX(calc(-50% + ${x}px)) translateY(${y}px) rotate(${angle}deg);z-index:${50-Math.abs(rel)}"><img src="${backPath()}" alt="抽一张牌"></button>`}).join('')}function fly(source,target,done){const a=source.getBoundingClientRect(),b=target.getBoundingClientRect(),clone=source.cloneNode(true);clone.className='fan-card flying-card';clone.style.cssText=`left:${a.left}px;top:${a.top}px;width:${a.width}px;height:${a.height}px;transform:none`;document.body.append(clone);requestAnimationFrame(()=>{clone.style.left=`${b.left}px`;clone.style.top=`${b.top}px`;clone.style.width=`${b.width}px`;clone.style.height=`${b.height}px`;clone.style.transform='rotate(0deg)'});setTimeout(()=>{clone.remove();done()},470)}function choose(button){if(cards.length>=3||button.disabled)return;button.disabled=true;const id=Number(button.dataset.id),orientation=Math.random()<.5?'upright':'reversed',target=$('spreadBoard').querySelectorAll('.slot-frame')[cards.length];new Image().src=cardPath(id);fly(button,target,()=>{cards.push({id,orientation});deckOrder=deckOrder.filter(x=>x!==id);renderBoard();if(cards.length===3)setTimeout(openReveal,260);else renderFan()})}function openReveal(){$('revealBoard').innerHTML=boardMarkup(true);$('revealBoard').querySelectorAll('.reading-card').forEach(card=>card.addEventListener('click',()=>{card.classList.add('revealed');card.dataset.open='1';if($('revealBoard').querySelectorAll('[data-open="1"]').length===cards.length)$('beginInterpret').disabled=false}));show('reveal')}function renderInterpret(){const item=cards[interpretIndex],p=positions[interpretIndex],x=identity(item.id);$('positionEn').textContent=p[1];$('positionZh').textContent=p[0];$('cardName').textContent=x.nameZh;$('positionPrompt').textContent=p[2];$('orientation').textContent=item.orientation==='upright'?'正位':'逆位';$('interpretCard').innerHTML=flipper(item);$('positionNote').value=notes[interpretIndex];$('nextInterpret').textContent=interpretIndex===2?'保存到我的手册':`继续到${positions[interpretIndex+1][0]}`;show('interpret')}function save(){const key='magic_u_reading_records_v1',records=(()=>{try{const x=JSON.parse(localStorage.getItem(key)||'[]');return Array.isArray(x)?x:[]}catch{return[]}})();records.unshift({id:`reading-${Date.now()}`,createdAt:new Date().toISOString(),spreadId:'time',spreadTitle:'时间之流',question:$('question').value.trim(),cards:cards.map((item,i)=>({...item,position:positions[i][0],nameZh:identity(item.id).nameZh,nameEn:identity(item.id).nameEn})),answers:[...notes]});localStorage.setItem(key,JSON.stringify(records.slice(0,100)))}function start(){deckOrder=shuffle();cards=[];notes=['','',''];interpretIndex=0;renderBoard();renderFan();show('draw')}$('startDraw').onclick=start;$('fanWrap').onclick=e=>{const b=e.target.closest('.fan-card');if(b)choose(b)};$('beginInterpret').onclick=()=>{interpretIndex=0;renderInterpret()};$('nextInterpret').onclick=()=>{notes[interpretIndex]=$('positionNote').value.trim();if(interpretIndex<2){interpretIndex++;renderInterpret()}else{save();show('done')}};$('again').onclick=()=>{$('question').value='';show('intro')};show('intro')})();
