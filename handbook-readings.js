(()=>{const key='magic_u_reading_records_v1',$=id=>document.getElementById(id);function read(){try{const x=JSON.parse(localStorage.getItem(key)||'[]');return Array.isArray(x)?x:[]}catch{return[]}}function date(value){const d=new Date(value);return Number.isNaN(d.valueOf())?'':d.toLocaleDateString('zh-CN',{year:'numeric',month:'2-digit',day:'2-digit'})}const records=read(),count=$('readingNotesCount'),list=$('readingArchiveList'),archive=$('readingArchiveCount');if(count)count.textContent=`${records.length} 篇`;if(!list)return;if(archive)archive.textContent=records.length?`${records.length} 篇`:'';if(!records.length){const p=document.createElement('p');p.className='archive-empty';p.innerHTML='完成一次占卜后，<br>牌阵和你的理解会留在这里。';list.append(p);return}records.forEach(record=>{const article=document.createElement('article');article.className='note-item';const time=document.createElement('time');time.textContent=`${date(record.createdAt)} · ${record.spreadTitle}`;article.append(time);if(record.question){const q=document.createElement('p'),strong=document.createElement('strong');strong.textContent='问题';q.append(strong,document.createElement('br'),document.createTextNode(record.question));article.append(q)}record.cards.forEach((card,i)=>{const p=document.createElement('p'),strong=document.createElement('strong');strong.textContent=`${card.position} · ${card.nameZh} · ${card.orientation==='upright'?'正位':'逆位'}`;p.append(strong,document.createElement('br'),document.createTextNode(record.answers[i]||'暂未写下理解'));article.append(p)});list.append(article)})})();
